# Test Dependencies
# Add these to your environment for running tests

# Note: Using compatible version ranges instead of pinned versions
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=4.0.0
pytest-mock>=3.10.0
